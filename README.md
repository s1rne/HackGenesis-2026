# Умный роутинг выплат

Маршрутизатор выплатного трафика между платёжными провайдерами: для каждой
заявки выбирает партнёра с учётом жёстких ограничений и нескольких
одновременно действующих целей распределения, обходит каскад при отказе,
объясняет каждое решение и по итогам прогона выдаёт аналитику с конкретными
рекомендациями.

Ruby, только стандартная библиотека. Ни одного внешнего гема, ни строчки
машинного обучения — все оценки считаются классической статистикой и
проверяются на бумаге.

Пользуются им четверо, и у каждого свой вопрос: дежурный по выплатам
спрашивает «сегодня всё нормально», партнёрский менеджер — «о чём говорить
с партнёром», поддержка — «почему выплата ушла туда», продуктовый —
«а если сделать иначе». Разбор ролей и решений по подаче — в
[docs/product.md](docs/product.md).

---

## Быстрый старт

```bash
ruby bin/route run                  # маршрутизация + отчёт
ruby scripts/validate_10.rb routing_decisions.json    # автопроверка организаторов
```

Больше ничего устанавливать не нужно: `bundle install` отсутствует как класс —
внешних зависимостей у проекта нет.

Нужен **Ruby 3.0 или новее** (проверялось на 3.4 и 4.0). Если запустить более
старым интерпретатором, обе точки входа скажут об этом человеческим текстом,
а не синтаксической ошибкой: проверка версии стоит до загрузки библиотеки.
На macOS системный `/usr/bin/ruby` — это 2.6, нужен `brew install ruby`.

### Команды

| Команда | Что делает |
|---|---|
| `ruby bin/route plan` | показать собранный конвейер: какие ограничения и цели включены, с какими весами |
| `ruby bin/route decisions` | маршрутизировать очередь → `routing_decisions.json` |
| `ruby bin/route report` | аналитика → `routing_report.json` |
| `ruby bin/route run` | оба файла за один прогон |
| `ruby bin/route compare` | прогнать одну очередь всеми профилями и сравнить распределения |
| `ruby bin/route replay` | контрфактический реплей истории: что сделал бы роутер на прошлых 100 операциях |
| `ruby bin/route explain op_103` | разбор одной заявки: кого рассматривали, почему отсеяли, почему выбран этот |
| `ruby bin/route finalize` | собрать `routing_decisions_test.json` и `routing_report_test.json` и проверить их перед сдачей |
| `ruby bin/route validate` | запустить автопроверку организаторов по собранным решениям |
| `ruby bin/dashboard` | HTML-дашборд объяснимости в `out/dashboard.html` |
| `rake test` | тесты |

Пути к данным переопределяются флагами: `--providers`, `--queue`, `--history`,
`--config`, `--overlays`, `--profile`, `--decisions`, `--report`.

---

## Как принимается решение

Порядок шагов один и тот же независимо от выбранной стратегии. Ниже — обзор;
формальное описание с формулами, разбором расчёта до последней цифры и
доказательством достижимости целей — в
[docs/decision-policy.md](docs/decision-policy.md).

```
заявка
  │
  ├─ 1. жёсткие ограничения ──────────── допуск: можно ли вообще отправить
  │      11 правил, каждое с кодом причины отказа
  │
  ├─ 2. недостижимые цели ────────────── доля недоступного перераспределяется
  │      факт уступки записывается в решение
  │
  ├─ 3. согласование целей ───────────── кого предпочесть среди допустимых
  │      8 целей, эшелоны, веса, явный тай-брейк
  │
  ├─ 4. каскад ───────────────────────── при отказе — следующий кандидат
  │
  └─ 5. пул исчерпан ────────────────── политика: повтор или self-провайдер
```

### 1. Жёсткие ограничения — допуск

Отвечают на вопрос «можно ли отправить эту заявку этому провайдеру».
Не сравнивают провайдеров между собой, не имеют веса: только допуск или отказ
с причиной. Порядок проверки задаётся конфигурацией, первое нарушение
прекращает проверку и попадает в отчёт как причина.

`lib/routing/constraints/`: статус, выделенная доля трафика, диапазон суммы,
банковский фильтр, дневной лимит оборота, верхнее обязательство по обороту,
лимиты заявок в работе, реквизиты, маржа, интенсивность, валюта.

Новое правило — новый класс-наследник и строчка в `config/routing.yml`.
Регистрация происходит самим фактом наследования; ни каскад, ни скоринг об
этом знать не обязаны.

### 2. Мягкие цели — предпочтение

Отвечают на другой вопрос: «кого предпочесть среди тех, кому уже можно».
Каждая цель возвращает величину в своих единицах — рубли недобора, доля
дефицита, конверсия, номер приоритета, — а сравнимыми их делает нормализация
по пулу кандидатов.

`lib/routing/strategies/`:

| Цель | Что делает |
|---|---|
| `count_share` | доля от количества заявок |
| `volume_share` | доля от объёма в рублях |
| `cascade_priority` | очередь в каскаде по `priority` |
| `amount_band` | предпочтение по диапазону суммы |
| `conversion` | приоритет по конверсии |
| `load_balance` | предпочтение менее загруженным |
| `turnover_commitment` | финансовые обязательства по обороту |
| `margin` | маржинальность для мерчанта |

Две вещи здесь сделаны иначе, чем напрашивается.

**Доли считаются дефицитом, а не жребием.** Бросать кубик с вероятностями
долей на десяти заявках бессмысленно: разброс получается в десятки процентов.
Вместо этого считается, сколько заявок провайдер уже должен был получить к
этому моменту (`цель × (N+1)`) и сколько получил на самом деле. Кто отстал
сильнее — тот и предпочтительнее. Это детерминированный аналог метода
наибольших остатков, разложенный на поток: доли сходятся к целевым с первых
же заявок, а провайдер, временно выпавший из-за жёсткого ограничения, вернёт
свою долю сам, без отдельного механизма компенсации.

**Конверсия оценивается нижней границей, а не наблюдаемой частотой.**
Провайдер с тремя успехами из трёх не лучше провайдера с девяноста из ста,
хотя «сырая» конверсия у первого выше. Нижняя граница интервала Вильсона
разводит их: 0.44 против 0.83. Заявленная `conversion_24h` работает как
априорная оценка весом в 20 виртуальных наблюдений и постепенно вытесняется
фактом прогона — провайдер, отказавший дважды подряд, теряет приоритет сразу.

### 3. Согласование конфликтующих целей

Цели регулярно указывают на разных провайдеров. Просто сложить веса
недостаточно: доля трафика и обязательство перед партнёром — величины разной
природы. Поэтому цели разложены по **эшелонам**, и старший решает раньше
младшего:

1. цели сортируются по эшелонам;
2. внутри эшелона считается взвешенная сумма нормализованных значений;
3. эшелоны сравниваются по очереди; если разрыв в старшем меньше
   `tier_epsilon`, кандидаты считаются равными и спор уходит вниз;
4. остаток ничьих разводит явная цепочка `tie_break` — результат
   воспроизводим и не зависит от порядка ключей во входном файле.

Режим задаётся настройкой `scoring.mode`: `weighted` — одна взвешенная сумма,
`lexicographic` — строго по эшелонам, `lexicographic_weighted` — по эшелонам
со взвешенной суммой внутри (по умолчанию).

Старший эшелон — сильное оружие, и включается он не всегда. Обязательство по
обороту даётся на сутки: «не добрано 2 млн» в девять утра и в одиннадцать
вечера — разные ситуации при одинаковом недоборе. Поэтому цель считает не
недобор, а требуемый от остатка суток темп, и молчит, пока провайдер идёт по
графику. Иначе любое плановое отставание забирало бы весь трафик с первой
заявки дня.

### 4. Недостижимые цели

Если провайдер, которому причитается доля трафика, не проходит жёсткие
ограничения, цель по нему выполнить нечем. Роутер не делает вид, что цели не
было: доля перераспределяется между доступными, а факт уступки уезжает в
решение (`events`) и в отчёт (`goal_relaxations`).

Отдельно считается, достижима ли цель **в принципе**. Для каждого провайдера
по структурным ограничениям выводится коридор:

* **потолок** — доля заявок, которые он вообще имеет право взять;
* **пол** — доля заявок, где он единственный, кто имеет право.

Цель вне коридора недостижима ни при какой политике, и это доказывается
пересчётом. На данных кейса недостижимы сразу две цели:

* `цель 25% на quickpay недостижима снизу, коридор 30..90%` — он единственный
  допустимый в 30% заявок, меньше отдать ему нельзя;
* `цель 35% на payflow недостижима сверху, коридор 10..30%` — у него осталось
  100 000 ₽ дневного лимита, это ровно три заявки из очереди.

Отсюда минимальное отклонение распределения от плана — **5.0 п.п.**,
независимо от того, как хорошо работает роутер. Фактически достигнуто ровно
5.0 п.п., то есть распределение оптимально. Раздел `target_achievability`
в отчёте; там же указано, какое именно ограничение связывает коридор.

Коридор считается в предположении, что каждая заявка уходит допустимому
внешнему провайдеру. При политике `exhausted_pool_policy: fallback` часть
заявок уходит на self-провайдера, и доли внешних могут опуститься ниже пола —
это не противоречие, а другая постановка задачи.

### 5. Каскад и fallback

При отказе провайдера в приёме заявка уходит следующему кандидату, пул
пересчитывается заново — состояние изменилось, и ранжирование вместе с ним.
Когда допустимых не осталось, применяется политика `exhausted_pool_policy`:

* `retry_best` (по умолчанию) — повторить на лучшем кандидате;
* `fallback` — уйти на self-провайдера.

Уход на self-провайдера при отказе по конверсии прятал бы проблему: маршрут
был, отказала выплата, а не маршрутизация, и аналитика по внешним партнёрам
после такой подмены перестала бы что-либо значить. На self-провайдера роутер
уходит тогда, когда допустимых нет вовсе.

### Как увидеть fallback своими глазами

На публичной очереди у каждой заявки есть хотя бы один допустимый провайдер —
это подтверждается эталоном организаторов, — поэтому пул ни разу не остаётся
пустым и `fallback_used` в отчёте равен нулю. Это не значит, что механизм не
работает: ему просто не на чем сработать.

```bash
ruby bin/route run --profile failover_demo \
  --decisions out/routing_decisions.failover.json \
  --report out/routing_report.failover.json
```

В этом профиле провайдеры отказывают часто, и каскад виден целиком:

```
op_101 -> spacepayments (approved)
  1  vipay          skipped   provider_timeout        ответ expired за 512 с
  2  payflow        skipped   provider_declined       ответ rejected за 85 с
  3  quickpay       skipped   provider_declined       других допустимых нет
  4  spacepayments  selected  fallback_self_provider  внешний пул пуст
```

Пять заявок из десяти уходят на self-провайдера. Готовая выгрузка лежит в
`out/routing_decisions.failover.json`.

---

### Пустой пул: два разных «пусто»

Когда допустимых провайдеров не осталось, важно, почему их не осталось.

* **По правилам.** Заявку не имеет права взять никто: не тот банк, не та
  сумма, отрицательная маржа. Структурное «пусто», и рассказывать о нём
  особо нечего.
* **По ёмкости.** По правилам партнёр подходит, но кончился дневной лимит,
  забиты слоты, упёрлись в интенсивность. Это другое «пусто», и оно означает
  деньги, которые партнёр сегодня уже не возьмёт.

В обоих случаях заявка уходит на собственного провайдера: дневной лимит стоит
в ТЗ среди hard-constraints, наравне со статусом и банковским фильтром, и
партнёр, который его выбрал, права на заявку больше не имеет. ТЗ на этом
месте не оставляет выбора: «Если пул пуст — fallback на spacepayments».

Разница между двумя «пусто» видна не в получателе, а в записи. Уход по
ёмкости поднимает тревогу:

* событие `capacity_alarm` в решении — с именем партнёра, ограничением,
  которое упёрлось, и его текущим состоянием;
* раздел `capacity_alarms` в отчёте — сколько заявок, на какую сумму, у кого
  и по какому ограничению;
* рекомендация с конкретным параметром, который предлагается поднять.

Без этого уход на себя выглядел бы как ровное распределение, и проблема
партнёра растворилась бы в собственном обороте. Обратное поведение —
«назвать того, кто упёрся в лимит, вместо собственного гейта» — доступно
настройкой `run.capacity_exhausted_policy: route_anyway`.

### Таймаут — не отказ

Единственное место, где текст ТЗ и практика расходятся прямо.

ТЗ: «при отказе/таймауте — исключить провайдера из пула и выбрать
следующего». Разбор кейса: таймаут не означает, что деньги не ушли; статус
выясняет статус-чек, а до него заявка считается принятой.

Оба прочтения рабочие, переключаются настройкой `run.timeout_policy`:

| значение | поведение |
|---|---|
| `pending_success` (по умолчанию) | каскад не продолжается, заявка остаётся у того, кто не ответил, отказ не засчитывается, здоровье партнёра не падает |
| `retry_next` | буквальное чтение ТЗ: таймаут приравнивается к отказу |

По умолчанию выбрана практика, потому что цена ошибки несимметрична: не
добрать долю партнёра неприятно, отправить выплату дважды — дорого.

«Считается успехом» проведено до конца: незавершённая заявка расходует
дневной лимит наравне с одобренной — иначе получилась бы дыра, в которую
партнёр незаметно взял бы больше оговорённого. А вот слот и реквизит
освобождаются: в проде их закрывает статус-чек, которого в модели нет, и
держать их до конца прогона значило бы запереть партнёра намертво после
нескольких таймаутов.

### Проверка чужой моделью

У ёмкости есть цена, которую легко не заметить. Скрипт организаторов считает
допустимость провайдера **по снимку** состояния, один раз, а роутер — по
накопленному, которое меняется от заявки к заявке, как и требует ТЗ
(«после каждой заявки обновлять загрузку, оборот, счётчики»). На короткой
очереди картины совпадают; на длинной расходятся.

Ни одна проверка структуры этого не видит: файл валиден, все заявки покрыты,
поля на месте. Поэтому логика проверяющего повторена внутри проекта
(`lib/routing/grader_check.rb`) и встроена в `bin/route finalize`:

```
OK  выбранный провайдер допустим по модели проверяющего
OK  совпали на 130 заявках, где допустим ровно один провайдер
```

Расхождение расхождению при этом рознь, и проверка их различает:

* расхождение, подкреплённое записанной тревогой `capacity_alarm`, —
  следствие ТЗ, а не ошибка. Оно попадает в `explained_by_capacity`
  и показывается отдельной строкой;
* расхождение без тревоги — ошибка, и файл не выпускается.

Это различие тоже проверено обратным ходом: если убрать из решений события,
то же самое расхождение снова становится ошибкой. Иначе объяснение
превратилось бы в способ замолчать что угодно.

### Здоровье провайдера

Конверсия отвечает на вопрос «как этот партнёр работает вообще». Здоровье —
на другой: «работает ли он прямо сейчас». Это разные вещи, и путать их дорого.

Считать серию отказов конверсией не выходит по арифметике: при двадцати
виртуальных наблюдениях два отказа подряд роняют оценку примерно на восемь
пунктов, и провайдер, у которого десять минут назад отвалился шлюз, успеет
получить ещё десяток выплат, прежде чем статистика его догонит.

Поэтому серия отказов — отдельная цель `provider_health` в старшем эшелоне.
Пока у всех ноль отказов подряд, она возвращает всем одно и то же, эшелон
остаётся ничейным и решение уходит вниз, к долям и конверсии. Как только
кто-то начинает падать, он проваливается в конец очереди — но **не
исключается**: если он единственный допустимый, заявка всё равно уйдёт ему.
Именно поэтому это цель, а не жёсткое ограничение: аварийный выключатель не
должен уметь отрезать последний оставшийся маршрут. Здоровье восстанавливается
со временем — отказ пятиминутной давности говорит о партнёре меньше, чем
отказ секунду назад.

### Проверка настроек до прогона

Конфигурация проверяется при загрузке: режим согласования, неотрицательность
порога и весов, известность ключей тай-брейка, политики исчерпания пула,
версия схемы. Все проблемы сообщаются разом, а не по одной за прогон.

```
Ошибка: конфигурация config/routing.yml негодна:
  - scoring.mode = "bananas", допустимо: weighted, lexicographic, lexicographic_weighted
  - scoring.tier_epsilon = -5, ожидалось неотрицательное число
```

---

## Объяснимость

Каждая попытка в `routing_decisions.json` несёт машинный код причины,
человеческую формулировку и конкретные числа:

```json
{
  "provider": "vipay",
  "decision": "skipped",
  "reason": "amount_exceeds_limit",
  "details": "150000 > limit_amount_max 100000",
  "stage": "hard_filter",
  "sequence": 1,
  "explanation": "сумма больше максимального чека провайдера"
}
```

Для выбранного провайдера причина — не «лучший скоринг», а конкретная цель,
которая решила спор с ближайшим соперником: берётся фактор с наибольшим
**отрывом**, потому что цель, по которой оба кандидата одинаково хороши,
ничего не объясняет. Рядом лежит полная раскладка скоринга: по каждому
фактору эшелон, вес, сырое значение, нормализованное, вклад и пояснение
словами.

### Разбор одной заявки

В проде вопрос к роутеру звучит не «какая у вас архитектура», а «почему эта
выплата ушла именно туда». Ответ должен читаться без чтения кода:

```bash
ruby bin/route explain op_101
```

```
Заявка op_101
  сумма 15000 RUB, банк sberbank
  итог: vipay — одобрено, 41 с

Кого рассматривали:
  --> vipay          отстаёт от целевой доли по объёму
      цель 46.4% по объёму; решающий фактор против payflow — volume_share
      payflow        не понадобился: заявку принял провайдер выше по каскаду
      quickpay       не понадобился: заявку принял провайдер выше по каскаду

Почему выбран именно он:
  Сравнивали с: payflow, quickpay
  Разбор скоринга победителя (эшелон / вес / вклад):
    эшелон 2  count_share    вес 1.00  вклад 1.000
              цель 40.0% по количеству, это первая заявка прогона
    ...
```

Команда читает готовую выгрузку, а не пересчитывает заново: разбирать надо
именно то решение, которое было принято, а не похожее на него. При
единственном допустимом провайдере раскладка скоринга не печатается —
сравнивать было не с кем, и таблица создавала бы видимость выбора.

Порядок массива `attempts` совпадает с порядком провайдеров во входном файле,
как в образце организаторов, а настоящая хронология каскада не теряется:
у каждой попытки есть `sequence`, а весь путь целиком — в `cascade.path`.

Коды причин собраны в одном каталоге (`lib/routing/reasons.rb`), поэтому в
выгрузке не может появиться причина, которую невозможно объяснить словами,
а в отчёте — причина без агрегата.

---

## Аналитика

`routing_report.json` содержит обязательные разделы (`period`,
`total_operations`, `distribution`, `skip_reasons`,
`projected_daily_utilization`, `recommendations`) и то, без чего по
распределению нельзя сделать ни одного вывода:

* `distribution` — факт, цель и **отклонение** по количеству и по объёму;
* `target_achievability` — достижимость целей, коридоры, причины блокировок;
* `outcomes` — одобрения, отказы, истечения, средняя задержка;
* `provider_performance` — заявленная и наблюдённая конверсия, расхождение;
* `cascade` — глубина, доля спасённых каскадом заявок, срабатывания fallback;
* `limits_at_risk` — что упрётся в потолок раньше остального;
* `goal_relaxations` — какие цели пришлось уступить и почему;
* `history_baseline` — база по истории с интервалами Вильсона;
* `data_quality` — замечания к входным данным и принятые допущения;
* `recommendations_detailed` — рекомендации с параметром, текущим и
  предлагаемым значением, основанием и приоритетом.

Рекомендация вида «стоит улучшить распределение» бесполезна: по ней нечего
сделать. Поэтому каждая запись называет конкретный параметр:

```json
{
  "text": "payflow выбрал 96.7% дневного лимита, свободно 100000 — снизить
           traffic_percentage до 1.2% или поднять daily_amount_limit",
  "parameter": "providers.payflow.traffic_percentage",
  "current": 35.0, "suggested": 1.2,
  "evidence": "оборот 2900000 при лимите 3000000",
  "priority": "high"
}
```

### Контрфактический реплей

`ruby bin/route replay` прогоняет 100 исторических операций через нашу
политику и сравнивает с тем, что было сделано на самом деле. Оценка даётся
**интервалом, а не точкой**: на решениях, совпавших с историей, берётся
фактический исход, а на разошедшихся фактического исхода не существует — там
считается интервал Вильсона по эмпирической конверсии выбранного провайдера.
Выдавать желаемое за действительное на таком сравнении слишком легко, поэтому
результат честно ограничен снизу и сверху.

---

## Конфигурация

Всё поведение задаётся `config/routing.yml`: состав и порядок правил, состав и
веса целей, эшелоны, режим согласования, цепочка тай-брейка, политика
исчерпания пула, параметры симуляции, пороги аналитики. Код об этих решениях
ничего не знает — он умеет только считать.

**Профили** — именованные наборы перекрытий поверх базовой конфигурации.
Одна и та же кодовая база даёт разное поведение без единой правки кода:

```bash
ruby bin/route compare                      # все профили на одной очереди
ruby bin/route run --profile conversion_first
```

| Профиль | Смысл |
|---|---|
| `balanced` | базовый: доли, конверсия и обязательства в балансе |
| `share_strict` | строго по целевым долям |
| `conversion_first` | максимум одобрений |
| `cascade_only` | классический каскад строго по `priority` |
| `capacity_saver` | беречь лимиты |
| `failover_demo` | демонстрация каскада и fallback |

`config/provider_overlays.yml` — поля, выведенные из истории
(`volume_share_pct`, `requests_per_minute_limit`, обязательства по обороту).
Отдельный файл, а не правка `providers.json`: входные данные организаторов
остаются нетронутыми, наши допущения видно отдельно, и отключаются они одним
флагом `--overlays /dev/null`. Обоснование каждого числа — в
[docs/calibration.md](docs/calibration.md).

---

## Тесты

```bash
rake test     # 354 теста, 2032 проверки
rake fuzz     # обстрел случайными очередями
rake check    # прогон + автопроверка организаторов + тесты
```

Minitest из стандартной библиотеки, без гемов и без Bundler. Покрыты все
одиннадцать жёстких ограничений по отдельности (срабатывает / не срабатывает
/ неприменимо), все восемь целей, согласование эшелонов, цепочка тай-брейка,
каскад с отказами и fallback, деградация недостижимой цели, откат состояния
после отказа, устойчивость загрузчика к пустым и битым файлам, и
побайтовая воспроизводимость двух прогонов подряд.

Отдельный приёмочный тест сверяет работу жёстких фильтров с эталоном
организаторов: в `data/reference_decisions.json` есть точный список
допустимых провайдеров для каждой из десяти заявок, и движок обязан
воспроизвести его один в один.

Три бага, найденные этими тестами и исправленные, описаны в истории
коммитов: потерянная запись об отказе при повторной попытке, коллизия
синонимов полей и недетерминированность часов при отсутствии времени
во входных данных.

### Обстрел случайными очередями

Тесты проверяют то, что мы придумали проверить. Боевую очередь пишем не мы,
поэтому есть отдельный обстрел: `tools/fuzz.rb` генерирует очереди по зерну —
от одной заявки до ста двадцати, с суммами на границах лимитов (0, 499, 500,
1000, 50 000, 100 001), с банками, которых нет ни у одного провайдера, с
пропущенными полями, чужими типами и временем вида `2026-13-45T99:99:99`.

Каждая очередь проходит семь утверждений: прогон не падает; решений ровно
столько же, сколько заявок; у каждой есть провайдер; выгрузка проходит
проверки сдачи; строгий вариант читается как JSON; в отчёте все обязательные
разделы; выбранный провайдер допустим по модели проверяющего. На чётных
зёрнах прогон повторяется и результат обязан совпасть побайтово.

```bash
rake fuzz                      # 200 очередей
CASES=5000 SEED=42 rake fuzz   # длинный прогон
ruby tools/fuzz.rb --seed 11 --cases 1   # повторить конкретную очередь
```

Обстрел нашёл одно настоящее расхождение: заявка с банком `SBERBANK`
уходила на payflow, потому что написание приводилось к общему виду, а
скрипт автопроверки сравнивает строки дословно и допустил бы для неё только
провайдера последней надежды. Способ сравнения стал настройкой
(`hard_constraints.bank_filter.spelling`), в конфигурации кейса стоит
дословный: расходиться с проверяющим дороже, чем не узнать банк в другом
регистре. Очередь-нарушитель сохраняется в `out/fuzz/`, повтор — по зерну.

---

## Матрица покрытия

Обе таблицы ТЗ построчно: семь стратегий распределения и восемь
hard-constraints — с файлом реализации и тестом, который это проверяет.
Имена тестов настоящие, их печатает `rake test`.

### Стратегии распределения

| № | Стратегия ТЗ | Реализация | Тест |
|---:|---|---|---|
| 1 | Процент от количества заявок (`traffic_percentage`) | `lib/routing/strategies/count_share.rb` | `test/strategies_test.rb`: 4 теста `test_count_share_*`, в том числе `..._follows_the_configured_target_not_the_current_count`; фактические доли прогона — `test/report_test.rb: test_distribution_counts_match_the_selected_providers_in_decisions` |
| 2 | Процент от объёма (`volume_share_pct`) | `lib/routing/strategies/volume_share.rb` | `test/strategies_test.rb`: 3 теста `test_volume_share_*`, в том числе `..._differs_from_count_share_on_the_same_history` |
| 3 | Очередь в каскаде (`priority`) | `lib/routing/strategies/cascade_priority.rb`, механизм попыток — `lib/routing/cascade.rb`, профиль `cascade_only` | `test/strategies_test.rb`: 3 теста `test_cascade_priority_*`; переход при отказе — `test/cascade_test.rb: test_refusal_of_the_first_provider_hands_the_operation_to_the_next` |
| 4 | По сумме чека | предпочтение — `lib/routing/strategies/amount_band.rb`, допуск — `lib/routing/constraints/amount_range.rb` | `test/strategies_test.rb`: 5 тестов `test_amount_band_*`, в том числе `..._is_a_preference_and_not_a_hard_limit` |
| 5 | Приоритизация по конверсии (`conversion_24h`) | `lib/routing/strategies/conversion.rb`, оценка — `lib/routing/statistics.rb` | `test/strategies_test.rb`: 4 теста `test_conversion_*`, в том числе `..._uses_the_lower_bound_so_a_small_sample_does_not_win`; сама формула — `test/statistics_test.rb`: 5 тестов `test_wilson_*` |
| 6 | По интенсивности (`requests_per_minute_limit`) | лимит — `lib/routing/constraints/rate_limit.rb`, предпочтение по загрузке — `lib/routing/strategies/load_balance.rb` | `test/constraints_test.rb`: 4 теста `test_rate_limit_*`, в том числе `..._forgets_requests_outside_the_window`; `test/strategies_test.rb`: 3 теста `test_load_balance_*`; `test/provider_state_test.rb: test_load_factor_includes_intensity_when_time_is_known` |
| 7 | По фин. обязательствам (`daily_turnover_min/max`) | нижнее — `lib/routing/strategies/turnover_commitment.rb`, верхнее — `lib/routing/constraints/daily_turnover_max.rb` | `test/strategies_test.rb`: 7 тестов `test_turnover_commitment_*`, в том числе `..._wakes_up_when_the_day_is_running_out`; `test/constraints_test.rb`: 3 теста `test_daily_turnover_max_*` |

Комбинация стратегий — не отдельный режим: все восемь целей работают
одновременно, конфликт разрешается эшелонами и весами. Механизм согласования
покрыт `test/scorer_test.rb` (эшелоны, `tier_epsilon`, цепочка тай-брейка,
воспроизводимость — 19 тестов).

### Hard-constraints

| Проверка ТЗ | Реализация | Тест |
|---|---|---|
| Статус провайдера | `lib/routing/constraints/provider_status.rb` | `test/constraints_test.rb`: 2 теста `test_provider_status_*` |
| Диапазон суммы чека | `lib/routing/constraints/amount_range.rb` | `test/constraints_test.rb`: 4 теста `test_amount_range_*` |
| Дневной max | `lib/routing/constraints/daily_amount_limit.rb` | `test/constraints_test.rb`: 4 теста `test_daily_amount_limit_*`, в том числе `..._does_not_lie_on_the_float_boundary` |
| In-progress count/amount | `lib/routing/constraints/in_progress_limits.rb` | `test/constraints_test.rb`: 5 тестов `test_in_progress_limits_*` |
| Банковский фильтр | `lib/routing/constraints/bank_filter.rb` | `test/constraints_test.rb`: 9 тестов `test_bank_filter_*` (белый список, чёрный список, оба прочтения `exclude_banks`, нормализация названий, политика для неизвестного банка) |
| Маржа | `lib/routing/constraints/margin.rb` | `test/constraints_test.rb`: 4 теста `test_margin_*`, включая `allow_negative_agreement` |
| Реквизиты | `lib/routing/constraints/requisites.rb` | `test/constraints_test.rb`: 3 теста `test_requisites_*` |
| Интенсивность | `lib/routing/constraints/rate_limit.rb` | `test/constraints_test.rb`: 4 теста `test_rate_limit_*` |

Все восемь дополнительно проверяются против эталона организаторов:
`test/acceptance_test.rb: test_hard_constraints_reproduce_the_reference_list_of_eligible_providers`
и `..._reproduce_the_reference_skip_reasons` — движок обязан воспроизвести
`data/reference_decisions.json` один в один.

Сверх таблицы ТЗ реализованы ещё три правила: `traffic_share` (3 теста),
`daily_turnover_max` (3 теста), `currency` (3 теста) — все в
`test/constraints_test.rb`. Состав и порядок реестра закреплены
`test_registry_knows_every_hard_constraint_of_the_case` и
`test_registry_builds_constraints_in_configuration_order`.

### Fallback и обновление метрик

| Требование ТЗ | Реализация | Тест |
|---|---|---|
| При отказе исключить провайдера и выбрать следующего | `lib/routing/cascade.rb` | `test/cascade_test.rb`: `test_refusal_of_the_first_provider_hands_the_operation_to_the_next`, `test_timeout_is_distinguished_from_a_decline_in_the_reason_code`, `test_max_attempts_bounds_the_cascade` |
| Если пул пуст — fallback на self-провайдера | `Cascade#use_fallback`, `run.exhausted_pool_policy` | `test/cascade_test.rb`: `test_empty_pool_falls_back_to_the_self_provider`, `test_fallback_is_used_when_hard_constraints_leave_nobody`, `test_no_route_at_all_is_reported_and_does_not_crash`, обе политики — `test_retry_best_policy_...`, `test_fallback_policy_...`, `test_the_two_policies_give_different_answers_on_the_same_input` |
| Обновление метрик после каждой заявки | `lib/routing/provider_state.rb` | `test/provider_state_test.rb`: 16 тестов, включая `test_decline_returns_everything_back_to_the_initial_values` и `test_a_long_chain_of_declines_leaves_no_leaked_limits` |
| Поведение при невыполнимой цели | `Cascade#relaxation_events`, `Fleet#count_target(among:)` | `test/cascade_test.rb`: `test_unreachable_target_share_produces_a_goal_relaxation_event`, `test_target_shares_are_recomputed_on_the_available_providers`, `test_reallocation_switch_changes_the_target_the_survivor_is_measured_against` |

### Аналитика

| Что | Реализация | Тест |
|---|---|---|
| Коридоры достижимости, пол, потолок по правилам и по деньгам | `lib/routing/analytics/achievability.rb` | `test/achievability_test.rb`: 11 тестов. Коридоры кейса сверяются с известными значениями, минимум проверяется на согласованность с фактическим отклонением, потолок по ёмкости — на то, что он берётся от запаса **на начало** прогона |
| Контрфактический реплей истории | `lib/routing/analytics/replay.rb` | `test/replay_test.rb`: 9 тестов. Полнота покрытия истории, интервальность оценки, совпавшие решения от фактического исхода, воспроизводимость, отказ работать на пустой истории |
| Расширяемость: новое правило и новая цель без правок ядра | реестры в `constraints/base.rb`, `strategies/base.rb` | `test/extensibility_test.rb`: 6 тестов. Классы объявляются прямо в тесте, включаются через конфигурацию и обязаны изменить маршрут и попасть в раскладку скоринга |
| Независимость от набора входных данных | весь конвейер | `test/alt_dataset_test.rb`: 16 тестов на втором наборе — другой шлюз, другие имена полей, другая валюта и банки, своя конфигурация |

---

## Устойчивость к входным данным


Решение не привязано к конкретному файлу. Поля ищутся по таблице синонимов
(`payment_system`, `provider_id`, `code` — одно и то же поле), регистр и
разделители не важны, суммы принимаются числом и строкой, список банков —
массивом и строкой через запятую. Незнакомые поля не выбрасываются, а
сохраняются и попадают в отчёт.

Загрузчик не падает на первом кривом поле: берётся разумное значение по
умолчанию, замечание записывается в `data_quality`, и видно, на каких
допущениях построен результат. Падение остаётся только там, где продолжать
нельзя — нет файла, не разбирается JSON, нет ни одной записи.

Отдельная тонкость, которую легко пропустить. В ТЗ поля названы парой —
«banks / exclude_banks, фильтр по банку», — и тип второго поля там не
определён; по названию оно читается как список исключений. В данных кейса
это **булев флаг**, задающий, как понимать `banks`: `false` — белый список,
`true` — чёрный. Именно так его трактует и скрипт проверки организаторов.
Поддерживаются оба прочтения: смысл определяется типом значения.

---

## Структура

```
bin/route                    точка входа CLI
bin/dashboard                генератор HTML-дашборда
config/routing.yml           все правила, веса и пороги
config/provider_overlays.yml поля, выведенные из истории
lib/routing/
  ingest/                    чтение данных, синонимы полей, замечания
  constraints/               жёсткие ограничения, по файлу на правило
  strategies/                мягкие цели, по файлу на цель
  analytics/                 отчёт, рекомендации, достижимость, реплей
  scorer.rb                  согласование конфликтующих целей
  cascade.rb                 порядок попыток, отказы, fallback
  provider_state.rb          состояние: резерв, расчёт, откат
  statistics.rb              Вильсон, наибольшие остатки, EWMA
  money.rb                   деньги целым числом копеек
test/                        тесты на minitest из стандартной библиотеки
docs/decision-policy.md      политика принятия решения: формулы, эшелоны,
                             тай-брейк, достижимость целей
docs/                        ТЗ, калибровка, исследование домена, критерии
WORKPLAN.md                  состояние работы, принятые решения и их причины
```

Деньги хранятся целым числом копеек не из педантизма: суммы лимитов и оборотов
складываются сотни раз за прогон, и на Float сравнение «оборот + сумма больше
дневного лимита» начинает врать ровно на границе — там, где это дороже всего.

---

## Зависимости и лицензии

**Внешних зависимостей нет ни одной.** В репозитории нет ни `Gemfile`, ни
`*.gemspec`, ни `vendor/`; `bundle install` не нужен и не предусмотрен. Всё,
что используется помимо самого языка, — модули стандартной библиотеки Ruby,
которые поставляются вместе с интерпретатором под лицензией Ruby / BSD-2-Clause.
Проприетарных компонентов и решений с закрытым исходным кодом в поставке нет,
как и обучаемых моделей: вся статистика классическая и проверяется на бумаге
(`lib/routing/statistics.rb`).

| Модуль | Где | Зачем |
|---|---|---|
| `json` | `lib/routing.rb`, `scripts/validate_10.rb` | чтение `providers.json` и очереди, запись `routing_decisions*.json` и `routing_report*.json` |
| `csv` | `lib/routing.rb` | разбор `data/operations_history.csv` для калибровки и реплея |
| `yaml` | `lib/routing.rb` | `config/routing.yml` и `config/provider_overlays.yml` |
| `time`, `date` | `lib/routing.rb` | отметки времени заявок, отчётный период, границы суток для обязательства по обороту |
| `digest` | `lib/routing/simulator.rb` | MD5 как детерминированный источник зерна: зерно зависит только от пары «заявка + провайдер + номер попытки», поэтому перестановка заявок не меняет исход. Не криптография |
| `fileutils` | `lib/routing/cli.rb`, `dashboard.rb` | создание каталога `out/` перед записью выгрузок |
| `rbconfig` | `lib/routing/cli.rb` | путь к текущему интерпретатору, чтобы `bin/route validate` запускал скрипт организаторов тем же Ruby, а не системным 2.6 |
| `erb` | `lib/routing/dashboard.rb` | шаблоны HTML-дашборда из `lib/routing/views/` |
| `optparse` | `bin/route`, `lib/routing/dashboard.rb` | разбор флагов командной строки |
| `minitest/autorun` | `test/test_helper.rb` | тесты |
| `tmpdir` | `test/test_helper.rb` | временные каталоги в тестах |

### Доля Ruby в репозитории

Считаются строки исходников: `*.rb`, `bin/route`, `bin/dashboard`, `Rakefile`
как Ruby; ERB-шаблоны дашборда и YAML-конфигурация — отдельно. Каталоги
`docs/`, `data/` и сгенерированные выгрузки в `out/` исключены, как и JSON —
это данные, а не код.

| Тип | Файлов | Строк | Доля |
|---|---:|---:|---:|
| Ruby (`*.rb`, `bin/route`, `bin/dashboard`, `Rakefile`) | 70 | 9027 | **81.9%** |
| ERB-шаблоны дашборда (`lib/routing/views/`) | 11 | 1539 | 14.0% |
| YAML-конфигурация | 3 | 451 | 4.1% |
| Итого | 84 | 11 017 | 100% |

ERB-шаблоны — это тоже Ruby с разметкой внутри; если считать их Ruby, доля
получается 95.9%. Мы приводим строгий вариант — он ниже и честнее. Из 9027
строк Ruby 212 приходятся на `scripts/validate_10.rb` — автопроверку
организаторов; без неё доля 81.6%, то есть на результат это не влияет.

Пересчитать самому:

```bash
ruby -e 'h=Hash.new(0); Dir.glob("**/*").each{|p| next unless File.file?(p);
  next if p.start_with?("docs/","data/","out/");
  k = (File.extname(p)==".rb" || %w[bin/route bin/dashboard Rakefile].include?(p)) ? "ruby" :
      File.extname(p)==".erb" ? "erb" : File.extname(p)==".yml" ? "yaml" : next;
  h[k] += File.readlines(p).size}; t=h.values.sum;
  h.each{|k,v| puts format("%-5s %6d %5.1f%%", k, v, 100.0*v/t)}'
```

---

## Сдача

```bash
ruby bin/route finalize
```

Собирает `routing_decisions_test.json` и `routing_report_test.json` в корне
репозитория из `data/operations_queue_test.json` и проверяет одиннадцать
условий сдачи: имена, место, структуру, покрытие всех заявок, обязательные
поля. Пока боевой очереди нет, команда собирает заготовку из публичной и
громко об этом сообщает.
